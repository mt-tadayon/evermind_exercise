name: evermindexercise
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^1.1.0

  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  fonts:
    - family: AbrilFatface
      fonts:
        - asset: lib/assets/fonts/AbrilFatface-Regular.ttf
          style: normal
  uses-material-design: true
  assets:
    - lib/assets/images/dashboard_background.jpg

